{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace fiddich build",
    "pub": "yarn workspace fiddich pub",
    "inspect": "yarn workspace inspect start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "typescript": "^4.9.5"
  }
}
